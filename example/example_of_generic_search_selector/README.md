# Generic Search Selector example

Demonstrates main pickers, nested `SubPickerTile` menus, async data sources,
radio modes, explicit delta persistence, and SearchAnchor-compatible popup
customization.

Run locally with:

```sh
flutter run -d chrome
```

The deployed example is available at
https://alexqwesa.github.io/generic_search_selector/.

# Future ideas

- `footerBuilder`
- `customActions`?
- `enum PopupThemes { glass, classic }`
- Don't keep external listen forever — only while popup is open; optional picker-owned listenable
- `editItem` callback
- If `MediaQuery` has enough space, offset sublist popup slightly below trigger
- Very small windows: fullscreen popups (e.g. `SmartAlertDialog(fullscreenBreakpoint: 600, ...)`)
- UX: users miss the search field — hint or icon
- Pass through more Material 3 `SearchAnchor` parameters
- Smaller customizations without full `itemBuilder` (icon, trailing icons)
- `selectedStyle` / `selectedWidget`

name: search_anchor_picker
description: >-
  A searchable Flutter picker with multi or single selection, nested
  sub-pickers, and async save callbacks that roll back on failure.
version: 0.2.2
homepage: https://github.com/Alexqwesa/search_anchor_picker
repository: https://github.com/Alexqwesa/search_anchor_picker
issue_tracker: https://github.com/Alexqwesa/search_anchor_picker/issues

environment:
  sdk: ^3.10.1
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.1.0

flutter:

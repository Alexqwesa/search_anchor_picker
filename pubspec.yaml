name: search_anchor_picker
description: >-
  Generic searchable picker (multi / single / singleOptional) with stable in-overlay
  selection, nested sub-pickers, async toggle gating, and lazy lifecycle-managed
  controllers and focus nodes.
version: 0.2.0
homepage: https://github.com/Alexqwesa/search_anchor_picker
repository: https://github.com/Alexqwesa/search_anchor_picker
issue_tracker: https://github.com/Alexqwesa/search_anchor_picker/issues

environment:
  sdk: ^3.10.1
  flutter: ">=3.38.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  very_good_analysis: ^10.1.0

flutter:

name: generic_search_selector
description: >-
  Generic searchable picker (multi-select / radio) with stable in-overlay
  selection, nested sub-pickers, and optional async toggle gating.
version: 0.0.3
homepage: https://github.com/Alexqwesa/generic_search_selector
repository: https://github.com/Alexqwesa/generic_search_selector
issue_tracker: https://github.com/Alexqwesa/generic_search_selector/issues

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_riverpod:

flutter:
